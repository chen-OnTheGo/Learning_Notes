

## 计算机网络的定义

- 计算机网络的精确定义并未统一。
- 最简单的定义是：一些==互相连接==的、==自治==的计算机的==集合==。
	- **互连**：是指计算机之间可以通过有线或无线的方式进行==数据通信==。
	- **自治**：是指==独立==的计算机，它有自己的硬件和软件，可以单独运行使用。
	- **集合**：至少需要两台计算机。
- 现阶段的计算机网络较好的定义：计算机网络主要是由一些==通用的==、==可编程的硬件互连==而成的，而这些硬件==并非专门==用来实现某一特定目的（例如，传送数据或视频信号）。这些可编程的硬件能够用来==传送多种不同类型的数据==，并能==支持广泛和日益增长的应用==。
	- 计算机网络所连接的硬件，并不限于一般的计算机，而是包括了智能手机等智能硬件。
	- 计算机网络并非专门用来传送数据，而是能够支持很多种的应用（包括今后可能出现的各种应用）。

e.g：下图则不是一个计算机网络，因为其中的终端机只具备输入输出功能，不是独立的计算机，不是自治的。只是一个运行分时系统的大型机系统。

![](../img/Pasted%20image%2020251210105944.png)



## 计算机网络的分类

#### 按交换技术分类：

- 电路交换网络
- 报文交换网络
- 分组交换网络

#### 按使用者分类：

- 公用网（公众网）
- 专用网：这种网络不向本单位以外的人提供服务。

#### 按传输介质分类：

- 有线网络：包括双绞线网络、光纤网络等。
- 无线网络：WiFi技术等。

#### 按覆盖范围分类：

- 广域网==WAN==（*Wide Area Network*）
- 城域网MAN（*Metropolitan Area Network*）
- 局域网==LAN==（*Local Area Network*）
- 个域网==PAN==（*Personal Area Network*）

其中，广域网又称远程网，是因特网的核心部分，其任务是为核心路由器提供远距离（例如，跨越不同的国家）高速连接，互连分布在不同区域的城域网和局域网。

城域网的覆盖范围一般是一个城市，通常作为城市骨干网，互连大量企业、机构和校园局域网。

LAN一般用微型计算机或工作站通过高速通信线路相连，速率通常在10Mbit/s以上，但地理上则局限在较小的范围内，如一个实验室、一个校园内。LAN通常由某个单位单独拥有、使用和维护。现在的一个学习或企业大都拥有许多个互连的局域网，这样的网络常称为*校园网*或企业网。

个域网是个人区域网络的简称，与上述网络不同的是，PAN**不是用来连接普通计算机的，而是在个人工作的地方把属于个人使用的电子设备**（e.g:便携式计算机、打印机、鼠标、耳机等）**用==无线技术==连接起来的网络**（其中，蓝牙是PAN的一种实现方式）。因此PAN也常称为无线个人区域网WPAN，其覆盖范围大概为10米。

需要注意的是，若中央处理机之间的距离非常近，e.g：仅1米的数量级甚至更小，则一般就称之为==多处理机系统==，**而不称它为计算机网络**。


#### 按拓扑结构分类：

- 总线型网络
- 星型网络
- 环型网络
- 网状型网络

其中，
##### 总线型网络

使用单根传输线把计算机连接起来。
![](../img/Pasted%20image%2020251210114001.png)
优点为建网容易、增减结点方便、节省线路。
缺点是重负载时通信效率不高，总线任意一处出现故障，则全网瘫痪。

##### 星型网络

将每个计算机都以单独的线路与中央设备相连。
（中央设备早期是计算机，后来是集线器，现在一般是交换机或路由器）
![](../img/Pasted%20image%2020251210114242.png)
优点：这种网络拓扑便于网络的集中控制和管理，因为端用户之间的通信必须经过中央设备。
缺点：成本高，中央设备对故障敏感。

##### 环型网络

将所有计算机的网络接口连接成一个环。（最典型的例子是令牌环局域网）
![](../img/Pasted%20image%2020251210114346.png)
环可以是单环，也可以是双环，环中信号是单向传输的。

##### 网状型网络

一般情况下，每个结点至少由两条路径与其他结点相连。多用在广域网中。
![](../img/Pasted%20image%2020251210114529.png)

其优点是可靠性高。
缺点：控制复杂、线路成本高。

##### 其他

需要注意的是，以上四种基本的网络拓扑还可以互连为更复杂的网络。