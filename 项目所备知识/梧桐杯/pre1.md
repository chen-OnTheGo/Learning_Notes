
（10折交叉验证）
try2.py中去掉gender属性，得分为：`0.889225619`
不去掉为：`0.88925369`。故不去掉。

在调整样本的权重上加上惩罚因子
`'scale_pos_weight': len(y[y==0]) / len(y[y==1]),  # 样本不平衡的惩罚因子`
得分为：`0.889225776`

把惩罚因子改成10，得分为：`0.889189`(去掉此步骤)

修改residence_base_station_id_rate为：
![](../../img/Pasted%20image%2020251021135029.png)

![](../../img/Pasted%20image%2020251021135059.png)

0.128  0.1267   0.13438  0.130917  0.132420

得分为：`0.8891690`


用`=IF([@age] < 18, 0, IF([@age] <= 27, 1, 2))`，分数为`0.889231821`

改为1，1.5，分数为



使用教育app的用户中校园用户占比：
![](../../img/Pasted%20image%2020251022152704.png)

使用娱乐app的占比：
![](../../img/Pasted%20image%2020251022152811.png)

购物app：
![](../../img/Pasted%20image%2020251022152901.png)

try2的基础上去掉traff_id，分数为：` 0.889231821`

try2的基础上测试最优样本权重，结果为：
`当前样本权重因子: 10.0, 最佳得分: 0.8892323486685244；最佳样本权重因子: 10.0, 最佳综合得分: 0.8892323486685244`
，再将其样本权重改为10，输出：
`0.889260965`,第二次输出：`0.8892039`(不稳定)
运行了一次try5，结果为：
`0.889260207`(结果不稳定)