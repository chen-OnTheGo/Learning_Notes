
再次被vim帅到。

问：IntelliJ IDEA中我用的vim模式，现在在某个文件中想进入某个方法被定义的地方，我该如何进入？

答：若用的IdeaVim，可以通过==`gd`==指令（*go to definition*），前提是光标在方法名上。

接着问：跳转后若要回到之前的文件位置该怎么办？

答：用Vim里面的跳转列表：==`Ctrl + O`==，这是Vim的"*jump back*"。